# Harden write-like-human use in indie-hacker-wrapup

- Rewrote Step 4 so the drafting step explicitly loads the `write-like-human` skill and applies its full 17-rule ruleset, then re-reads the draft against those rules before output.
- Why: the prior wording only said "apply the ruleset" with an inline summary, so on a weak session the summary got trusted instead of the full rules. This closes that reliability gap.
