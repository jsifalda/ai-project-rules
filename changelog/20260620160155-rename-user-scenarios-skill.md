# Rename skill `user-scenarios-setup` → `setup-user-scenarios`

- Renamed the skill directory and frontmatter `name` to match the repo's `setup-*` prefix convention used by all other bootstrapping skills.
- Updated references in `setup-aiengineering` (description, Modules table, report line) and the README skills table (reordered into the `setup-*` cluster).
- Slash command changes from `/user-scenarios-setup` to `/setup-user-scenarios`. No behavior change.
