# Add create-svg-logo skill

- New skill `create-svg-logo` — turns a brand description into multiple SVG logo concepts, layout lockups, and colour variations, plus a usage-guidelines document.
- Body split across four `references/` files so `SKILL.md` stays inside the 500-line guidance and detail loads only when the workflow reaches it.
- Description carries a negative trigger for banners, cards, OG images, and posters, so it does not compete with `create-svg-image` for the same requests.
- README skills table updated with the new row.
