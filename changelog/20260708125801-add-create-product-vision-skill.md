# Add create-product-vision skill

- Added the `create-product-vision` skill: turns a short product or project description into one tight, motivating vision doc covering three angles (motivation, practical, product).
- Finished a half-done rename: the directory was already `create-product-vision` but the frontmatter still said `product-vision`, breaking the name-matches-directory rule from the agentskills spec.
- Registered it in the README skills table with its `write-like-human` dependency.
