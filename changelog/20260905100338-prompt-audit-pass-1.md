# Cut dated prompting patterns from the core rules

- Removed caps boosters, `!!`, a doubled negation, and four restatements whose content already
  lives elsewhere in the same surface. Renamed the `No Laziness` rule to `Root Cause, Not Stopgap`
  and updated both places that referenced it.
- Why: `git blame` dates the oldest layer of `rules/general.md` to 2025-12-12 — Claude generations
  before adaptive thinking. On current models that text is not neutral: caps boosters cause
  over-triggering, and an anxious register produces a hedging model.
- Register only. No rule changes what it requires. The behavior-changing findings from the same
  audit are held for a second pass.
