# Let the model invoke better-plan

- Removed `disable-model-invocation: true` and the "Slash-only." wording from the skill and its README row.
- Widened the description trigger to natural-language requests, and told the preface where to take the input when no slash text exists.
- Why: the user wants better-plan to auto-trigger and be loadable by the Skill tool. The Stage 3 plan approval still gates execution and the PR.
