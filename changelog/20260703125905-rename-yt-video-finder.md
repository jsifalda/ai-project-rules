# Rename `youtube-video-finder` skill to `yt-video-finder`

- Renamed the skill directory and its `name:` field to the shorter `yt-video-finder`.
- Added the missing `README.md` Skills table row (the skill was previously untracked and never listed).
- Why: shorter, easier-to-invoke name; bring the skill in line with repo standards (validator passes, README parity).
