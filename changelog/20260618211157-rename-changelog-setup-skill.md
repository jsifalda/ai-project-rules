# Rename `changelog-setup` skill to `setup-changelog`

- Renamed the skill directory and its `name:` frontmatter so the slash command is now `/setup-changelog`.
- Updated the README skills table (moved the row to keep alphabetical order) and the `user-scenarios-setup` cross-reference to point at the new name.
- Why: verb-first naming reads better and matches the existing `setup-skills-autorefresh` skill.
