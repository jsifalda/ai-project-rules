# Architecture

> Recap doc — the 10,000ft view of how this system is built, derived from the ADRs in
> `docs/adr/`. Keep it in sync as ADRs are added or superseded. ADRs hold the *why* and the
> discarded options; this doc holds only the **current state**.

## Overview

<What this system is, its main responsibilities, and the shape of it in a few sentences.>

## Key components

- **<Component>** — what it does, why it exists.

## Cross-cutting decisions

<Standing choices that shape the whole system (language, data store, auth, error handling),
each linking the ADR that established it — see `YYYY-MM-DD-slug`.>

## Conventions

<Patterns the codebase reuses by default — the proven patterns that do NOT need a new ADR.>
