# Rename the `opusplan` skill to `op`

- Renamed the skill so it is invoked as `/op` instead of `/opusplan`: updated `name`, the slash-command mention, and the heading in `SKILL.md`, updated the README Skills-table row, and deleted the old `skills/opusplan/` dir.
- Why: shorter, faster to type.
- Left the line referencing Claude Code's built-in `opusplan` setting unchanged — that names a real Claude Code feature, not this skill.
