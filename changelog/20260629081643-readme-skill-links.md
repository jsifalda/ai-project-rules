# Link README skills table to each SKILL.md

- Made every skill name in the `## Skills` table a relative link to its `skills/<name>/SKILL.md`, so the table works as a clickable index.
- Updated the keep-in-sync note and the Contributing instruction so future rows are added in the linked form.
- Why: readers can now jump straight from the table to a skill's source instead of hunting for the directory.
