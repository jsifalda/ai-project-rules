# better-plan no longer recaps the run

- Removed the `Recap — Explain the run` section from `better-plan`, and the Stage 4, Stage 5,
  and `description` text that pointed at it.
- The plan file now holds only the routed plan the user approves. The flow ends at the Stage 5
  ship report.
- The recap repeated what the user had just read in the chat, and it pushed the routing table
  further from the top of the plan file.
- Kept the `README` skills table in sync.
